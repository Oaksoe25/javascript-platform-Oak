"use strict";Object.defineProperty(exports, "__esModule", { value: true });exports.default = void 0;
var _config = await jitiImport("prisma/config"); // prisma.config.ts (at project root)
var _default = exports.default =
(0, _config.defineConfig)({
  schema: 'prisma/schema.prisma',

  // NOTE: this must be "datasources" (plural) for the CLI/types to match
  datasources: {
    db: {
      provider: 'sqlite',
      // hard-coded for now so we avoid env issues while debugging
      url: 'file:./dev.db'
    }
  }
}); /* v9-5e908408ec92d1e3 */
