<% layout('layouts/main') %>
<h2>Users</h2>
<a class="btn" href="/users/new">+ Add User</a>
<ul class="list">
  <% items.forEach(u => { %>
    <li><strong><%= u.name %></strong> — <%= u.email %></li>
  <% }) %>
</ul>
